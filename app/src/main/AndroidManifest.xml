<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required on targetSdk 30+ for PackageManager.getPackageInfo() to see Shizuku. -->
    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:name=".SleepTimerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SleepTimer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.SleepTimer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="dev.xitee.sleeptimer.core.service.SleepTimerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Quick Settings tile: starts the timer with the preset duration. The
             tile label/icon here are what the user sees in the QS edit panel.
             TOGGLEABLE_TILE deliberately keeps the tile tappable from the lock
             screen: starting or cancelling the sleep timer on a nightstand must
             not require an unlock. -->
        <service
            android:name=".tile.SleepTimerTileService"
            android:exported="true"
            android:icon="@drawable/ic_timer"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- Optional soft screen-lock via GLOBAL_ACTION_LOCK_SCREEN. exported="true"
             is safe: only the system holds BIND_ACCESSIBILITY_SERVICE, and it only
             binds after the user enables the service in accessibility settings. -->
        <service
            android:name="dev.xitee.sleeptimer.core.service.screen.LockAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_lock_service" />
        </service>

        <!-- Per-instance widget configuration, launched by the widget host on
             placement (pre-Android 12) and via long-press -> edit (12+). Exported
             because some launchers start it directly rather than through the
             system's AppWidget grant mechanism. -->
        <activity
            android:name=".widget.SleepTimerWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.SleepTimer">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Home screen widget. exported=false is safe: APPWIDGET_UPDATE is a
             protected system broadcast (delivered regardless), and it keeps the
             internal WIDGET_START_TIMER trampoline action unreachable for other
             apps — only our own PendingIntent can trigger it. -->
        <receiver
            android:name=".widget.SleepTimerWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_sleep_timer_info" />
        </receiver>

        <receiver
            android:name=".receiver.SleepTimerDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>
