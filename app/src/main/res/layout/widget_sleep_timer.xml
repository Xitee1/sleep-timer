<?xml version="1.0" encoding="utf-8"?>
<!-- RemoteViews layout: only RemoteViews-compatible views and static attributes.
     Default texts are what the widget-picker preview (previewLayout) shows; the
     renderer overwrites them on every real update. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="8dp">

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_timer"
        android:tint="@color/widget_accent" />

    <TextView
        android:id="@+id/widget_minutes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:maxLines="1"
        android:text="@string/widget_preview_minutes"
        android:textColor="@color/widget_text"
        android:textSize="18sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="2"
        android:text="@string/widget_tap_to_start"
        android:textColor="@color/widget_text_secondary"
        android:textSize="12sp" />

</LinearLayout>
