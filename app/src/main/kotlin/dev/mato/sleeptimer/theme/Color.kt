package dev.mato.sleeptimer.theme

import androidx.compose.ui.graphics.Color

// Midnight palette — calm, dark, lavender accent
val MidnightBgTop = Color(0xFF2A2550)
val MidnightBgMid = Color(0xFF1A1830)
val MidnightBgBot = Color(0xFF0F0D1F)

val Accent = Color(0xFFC9B8FF)
val AccentDeep = Color(0xFF6B5BD6)
val AccentGlow = Color(0x40C9B8FF) // 0.25 alpha

val TextPrimary = Color(0xFFFFFFFF)
val TextDim = Color(0x8CFFFFFF) // 0.55 alpha
val TextMuted = Color(0x73FFFFFF) // 0.45 alpha
val TextFaint = Color(0x4DFFFFFF) // 0.30 alpha

val Surface1 = Color(0x0AFFFFFF) // 0.04 alpha — settings rows, chips
val Surface2 = Color(0x10FFFFFF) // 0.06 alpha — secondary buttons
val SurfaceStroke = Color(0x1AFFFFFF) // 0.10 alpha — borders
